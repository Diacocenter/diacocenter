import React from "react";
import Button from "@mui/material/Button";

export default function SideBarHeader() {
    return (
        <div>
            <Button variant="contained">side bar header</Button>
        </div>
    )
}